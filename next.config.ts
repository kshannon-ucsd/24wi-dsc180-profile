/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/24wi-dsc180-profile',  // Replace with your actual repo name
}

module.exports = nextConfig